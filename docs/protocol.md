# BC-768 BLE プロトコル（Android HCI キャプチャからの解析結果）

Health Planet (Android) と BC-768 の Bluetooth HCI キャプチャを解析して得た通信仕様。
キャプチャそのもの、UUID 実値、端末識別子、測定値は本ドキュメントに含めない（すべて git 管理外に置く）。

解析対象は 4 セッション、再構成できたメッセージ 68 件すべてでチェックサムが一致した。

## 1. Pairing は不要

**最も重要な発見。BC-768 との通信に BLE の Pairing / Bonding は必要ない。**

| 観測 | 内容 |
| --- | --- |
| `HCI_Encryption_Change` | キャプチャ全体で **0 件**。Android 側も暗号化していない |
| SMP | BC-768 → Android の `Security Request` (`0x0B`, AuthReq=`0x01`) が接続ごとに 1 回。**Android は応答していない** |
| ATT | すべて平文。認証エラー (`InsufficientAuthentication`) は 1 件も発生していない |

BC-768 は接続直後に Security Request を送るが、これを無視したまま GATT 通信が最後まで成立している。
macOS 側の検証（`verification-log.md`）で Pairing UI が出ず、Notify 購読も素通りしたのと整合する。

「Pairing 待機状態」は BLE の Pairing ではなく、**独自プロトコルのハンドシェイクを受け付ける状態**を指す。

## 2. 使用する属性

Android は Service 内の 5 つの Characteristic のうち、実際には 2 つしか使っていない。

| ATT handle | 役割 | 使用回数 |
| --- | --- | --- |
| `0x0017` | Notification 送信元（`NOTIFY_CHAR` のいずれか 1 本） | 75 |
| `0x0018` | `0x0017` の CCCD。`0x0001` を Write Request で書いて Notify 有効化 | 4 |
| `0x001A` | Write Command 送信先（= `WRITE_CHAR_1`） | 58 |

キャプチャに GATT Discovery が含まれていない（Android がキャッシュ済み）ため、
**handle と UUID の対応は未確定**。`WRITE_CHAR_1` / `WRITE_CHAR_2` のどちらへ書くかは実機で確かめる必要がある。
Notify 側は 3 本すべて購読しておけばどれに来ても受信できる。

書き込みはすべて Write Command（Write Without Response）。ATT MTU は既定の 23 のまま
（MTU 交換なし）なので、1 パケットのペイロードは 20 バイト。

## 3. フレーム構造

Characteristic 1 回の書き込み / 通知は、メッセージのフラグメント 1 個に相当する。

```text
[fragment_offset : 2 bytes, big endian]
[message_seq     : 1 byte ]
[fragment_length : 1 byte ]
[fragment_data   : fragment_length bytes]   ← 最大 16 バイト
```

- `fragment_offset` は再構成後メッセージ内のバイトオフセット。`0x0000`, `0x0010`, `0x0020` … と 16 ずつ増える。
- `message_seq` は複数フラグメントに分かれるメッセージを束ねる通番。1 フラグメントで収まる場合は `0x00`。
- 受信側は `fragment_offset` 順に `fragment_data` を連結する。

## 4. メッセージ構造

フラグメントを連結すると以下になる。

```text
[total_length : 2 bytes, big endian]   ← command 以降のバイト数（checksum を含む）
[command      : 2 bytes, big endian]
[payload      : total_length - 3 bytes]
[checksum     : 1 byte]
```

### チェックサム

```text
checksum = 0xFF - (sum(total_length .. payload の全バイト) & 0xFF)
```

言い換えると、**メッセージ全バイトの総和の下位 8 bit が必ず `0xFF` になる**。
再構成した 68 メッセージすべてでこの式が成立した。

## 5. コマンド

応答コマンドは要求コマンドに `0x8000` を立てたもの（`0x1000` 系は `0x9000`、`0x3000` は `0xB000`）。
正常応答の payload は `0x0000`。

| 方向 | command | payload | 意味（推定） |
| --- | --- | --- | --- |
| → | `0x0003` | ASCII 36 文字の UUID 文字列 | クライアント識別子の提示。ハンドシェイクの開始 |
| ← | `0x8003` | `0000` | 受理 |
| → | `0x0010` | 9 バイト（TLV） | セッション情報の設定 |
| ← | `0x8010` | `0000` | 受理 |
| → | `0x0020` | `00` | デバイス情報の要求 |
| ← | `0x8020` | 57 バイト（TLV。モデル名 `BC-768` を含む） | デバイス情報 |
| → | `0x1000` | `00` | データ取得要求 |
| ← | `0x9000` | 43 バイト（TLV） | データ応答 |
| → | `0x1002` | 64 バイト（TLV） | データ書き込み / 設定 |
| ← | `0x9002` | 43 バイト（TLV） | 応答 |
| → | `0x2010` | `00` | **測定の開始** |
| ← | `0xA010` | `0000` | 測定完了。応答まで 9〜12 秒（この間に体組成計へ乗る） |
| → | `0x3000` | `00` | 完了通知 |
| ← | `0xB000` | `0000` 〜 `0003` を観測 | 受理。値は**保持している測定の件数** |
| → | `0x3010` | `01` 〜 `03` を観測 | **測定結果の取得**。payload は**レコード番号**（1 が最新） |
| ← | `0xB010` | 87 バイト（TLV） | **測定結果**。先頭 2 バイトの下位が要求した番号と一致する |
| → | `0x0001` | `00` | セッション終了 |
| ← | `0x8001` | `0000` | 受理 |

### 測定を伴わないセッション（約 6 秒）

```text
LE Connection Complete
  ← SMP Security Request        （無視してよい）
  → Write Request  CCCD = 0x0001（Notify 有効化）
  → 0x0003 <client uuid>   ← 0x8003 0000
  → 0x0010 <日時 9 bytes>   ← 0x8010
  → 0x0020 00              ← 0x8020 <device info>
  → 0x1000 00              ← 0x9000 <data>
  → 0x1002 <64 bytes>      ← 0x9002 <data>
  → 0x3000 00              ← 0xB000 0000
  → 0x0001 00              ← 0x8001
Disconnect (reason 0x08)
```

### 測定を伴うセッション（約 15〜18 秒）

```text
  → 0x0003 … 0x1002 まで同じ
  → 0x2010 00              ← 0xA010 0000   ここで 9〜12 秒待つ（乗って測定）
  → 0x3000 00              ← 0xB000 0001   保持件数が返る
  → 0x3010 01              ← 0xB010 <87 bytes 測定結果>
  → 0x0001 00              ← 0x8001
Disconnect (reason 0x08)
```

### 複数件が溜まっているセッション

本体だけで 3 回測ってから Health Planet で同期したときの流れ。

```text
  → 0x0003 … 0x1002 まで同じ
  → 0x3000 00              ← 0xB000 0003   3 件保持
  → 0x3010 03              ← 0xB010 <header=0x0003  23:50:52 の測定>
  → 0x3010 02              ← 0xB010 <header=0x0002  23:51:21 の測定>
  → 0x3010 01              ← 0xB010 <header=0x0001  23:51:37 の測定>
  → 0x0001 00              ← 0x8001
Disconnect (reason 0x08)
```

- `0x3010` の payload は**レコード番号**。1 起点で、**1 が最新**、数字が大きいほど古い。
- Health Planet は件数 N から `N → 1` の順、つまり**古い方から**取っていく。
- `0xB010` の先頭 2 バイトの下位バイトが、要求した番号と一致する。
- `0xB000` が `0000` のとき、Health Planet は `0x3010` を**一度も送らない**。
  送っても前回値（残留値）が返るだけで、意味のあるデータにはならない。

切断は毎回 BC-768 側のタイムアウト（`reason=0x08`）で、正常終了でも同様。

## 6. payload の TLV

payload は `<tag:2 bytes><value>` の並び。tag の先頭バイトは `0x6A` / `0x7E` / `0x60` / `0x61` / `0x6F`。
value 長は tag ごとに固定。`0x60` 系は測定結果（`0xB010`）にのみ現れる。

### 日時（解読済み）

`0x0010` の payload はこの 2 つだけで構成される。

| tag | 長さ | 意味 |
| --- | --- | --- |
| `6A 32` | 2 bytes BE | **2000-01-01 起点の日数** |
| `6A 33` | 3 bytes BE | **当日 00:00 からの経過秒 × 2**（0.5 秒単位） |

HCI キャプチャ 6 サンプルすべてで、パケットの送信時刻と秒単位まで一致することを確認した。
つまり `0x0010` は日時設定コマンドである。

```text
2026-08-18 23:39:59 → 6a32 25fe  6a33 02999e   (9726 日, 170398 = 85199 秒 × 2)
2026-08-19 01:53:08 → 6a32 25ff  6a33 003508   (9727 日,  13576 =  6788 秒 × 2)
```

実装は `Sources/BC768Protocol/DateTime.swift`、期待値は `Tests/BC768ProtocolTests` で固定してある。

### 測定結果 `0xB010` の先頭 2 バイト

レコード番号が下位バイトに入る。上位バイトは通常 `0x00`、既に引き取り済みの残留値を返すときに
`0x05` を観測している（`0x0501`）。

### 測定結果 `0xB010` の構造（解読済み）

先頭 2 バイトのヘッダ（`0001`）の後に TLV が 23 個並び、合計 87 バイトになる。
下表の長さを積算すると 87 バイトに誤差なく一致する。

| tag | 長さ | 意味 | 単位 | 確度 |
| --- | --- | --- | --- | --- |
| `6A32` | 2 | 日付（2000-01-01 起点の日数） | 日 | 確定 |
| `6A33` | 3 | 時刻（当日 0 時からの 0.5 秒単位） | — | 確定 |
| `6A38` | 1 | 不明（観測値は常に `00`） | — | 未解析 |
| `6A3E` | 2 | 身長 | 0.1 cm | 確定 |
| `6A3B` | 1 | 不明（観測値は常に `00`） | — | 未解析 |
| `6021` | 2 | **体重** | 0.01 kg | 確定 |
| `6056` | 2 | **BMI** | 0.1 | 確定 |
| `6022` | 2 | **体脂肪率** | 0.1 % | 確定 |
| `6070` | 1 | 判定値?（体脂肪率の直後） | — | 未解析 |
| `6023` | 2 | **筋肉量** | 0.01 kg | 確定 |
| `6029` | 2 | **推定骨量** | 0.01 kg | 確定 |
| `6024` | 1 | 判定値または筋肉スコア? | — | 未解析 |
| `6077` | 1 | 判定値または筋肉スコア? | — | 未解析 |
| `605A` | 1 | 判定値?（基礎代謝の直前） | — | 未解析 |
| `6027` | 2 | **基礎代謝** | kcal | 確定 |
| `602F` | 2 | 不明（観測値は常に `0008`） | — | 未解析 |
| `6028` | 1 | **体内年齢** | 歳 | 確定 |
| `6025` | 2 | **内臓脂肪レベル** | 0.1 | 確定 |
| `607D` | 1 | 判定値? | — | 未解析 |
| `6F21` | 2 | **体水分量** | 0.1 kg | 確定 |
| `6F22` | 2 | 不明（ほぼ一定。12984〜12992） | — | 未解析 |
| `614B` | 2 | 抵抗 R? | 0.1 Ω | 推定 |
| `614C` | 2 | リアクタンス Xc?（観測値は負） | 0.1 Ω | 推定 |

### 「確定」の根拠

答え合わせをせずに確定できたものは、いずれも**物理的・生理学的な関係が独立に成立する**ことで裏づけた。
係数を 1 桁でも間違えると関係が崩れるため、値の妥当性だけに頼らずに判断できる。

```text
BMI        = 体重 / 身長²
除脂肪量   = 体重 × (1 - 体脂肪率) ≒ 筋肉量 + 推定骨量
体水分量   / 除脂肪量 = 0.72〜0.75      ← 除脂肪組織の水分比率
基礎代謝   / 筋肉量   = 30.1 kcal/kg
```

実装は受信ごとにこの 4 本を検算し、結果をログと JSON へ出す
（`Sources/BC768Protocol/Measurement.swift`）。

#### `6F21` は「率」ではなく「量」

当初 `6F21`（観測値 483〜494）を体水分率 48.3〜49.4 % と読んでいたが、
除脂肪量との関係が合わなかった。両方の解釈を 4 測定で比べた結果は次のとおり。

| 解釈 | 除脂肪量との比 | 判定 |
| --- | --- | --- |
| 体水分量 48.3〜49.4 kg | **72.4〜74.5 %** | 除脂肪組織の水分比率（72〜74%）と一致 |
| 体水分率 48.3〜49.4 % | 64 % 前後 | 標準から外れる |

**体水分量（kg）が正しい。**

#### `6027` は筋肉量から計算されている

```text
1902 / 63.10 kg = 30.143 kcal/kg
1913 / 63.50 kg = 30.126
1895 / 62.90 kg = 30.127
1896 / 62.90 kg = 30.143
```

4 測定で ±0.02 % 以内に収まる。ほぼ完全な線形関係で、基礎代謝として確定できる
（除脂肪量あたりでは 28.6 kcal/kg）。

#### Health Planet の表示との答え合わせ

アプリの表示（2026-08-19 03:23 の測定）と突き合わせ、次を確定させた。

| 項目 | アプリ表示 | 対応する tag |
| --- | --- | --- |
| 内臓脂肪レベル | 13.5 | `6025` = 135 |
| 基礎代謝量 | 1895 kcal | `6027` = 1895 |
| 体内年齢 | 39 才 | `6028` = 39 |

体重・体脂肪率・筋肉量・推定骨量・BMI も表示どおりで、検算による確定が裏づけられた。

**体水分率はアプリでも「-」と表示され、項目として存在しない。**
`6F21` を体水分率と読んでいたのは誤りで、体水分量（kg）が正しいことがここでも裏づけられた。

アプリには**「筋肉スコア」**という項目があり、この測定では 3 だった。
未解析タグの `6024` と `6077` がどちらも 3 なので、いずれかが該当すると思われるが、
どの測定でも両方 3 のままで区別できていない。

#### 残る推定

`614B` / `614C` は BIA のインピーダンス成分と思われる。
`614B` / `614C` は BIA のインピーダンス成分と思われる。

```text
R  = 374.5〜388.1 Ω
Xc = -43.8〜-49.0 Ω
```

BC-768 は足-足式で下半身のみを測るため、この範囲は妥当。
R が下がると体水分量が増える傾向も BIA の原理と一致する。ただし裏づけが取れていないので推定に留める。

### 設定応答 `0x9000` の構造

先頭 1 バイトのヘッダの後に TLV が 11 個並び、合計 43 バイト。同じ長さ表で誤差なく解釈できる。
`6A3E`（身長）が含まれ、他に `6A3C` `6A37` `6A38` `6A3B` `604F` `6A13` `6A15` `7E22` が並ぶ
（ユーザー設定と思われるが未解析）。

## 7. macOS 実装への含意

1. **Pairing を成立させる必要はない。** 仕様書 Phase 2 / Phase 3 は目的として不要になった。
2. Notify 3 本を購読したうえで、`WRITE_CHAR` へ `0x0003` から始まるハンドシェイクを送れば応答が返るはず。
3. 送信先が `WRITE_CHAR_1` / `WRITE_CHAR_2` のどちらかは実機で確認する。誤った方へ書いても、
   送るのは HCI ログで確認済みの正規 payload であり、推測データではない。
4. クライアント識別子（`0x0003` の 36 文字）は Android アプリが生成した UUID。
   macOS から別の UUID を送って受理されるかは未検証。
