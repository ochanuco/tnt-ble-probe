# 検証ログ

UUID の実値と Peripheral 識別子は記載しない（論理名 / `<masked>` で表記する）。

## 2026-08-19 初回検証

### 環境

| 項目 | 値 |
| --- | --- |
| macOS | 26.6.1 (25G76) |
| Swift | 6.3.3 / Xcode 26.6 |
| 対象 | TANITA BC-768 |
| コマンド | `bc768-probe scan` / `bc768-probe probe --debug --wait 45` |

BC-768 側の「設定/通信」ボタン長押しは**未実施**（本体は広告を出している状態だった）。

### scan

Service UUID フィルタあり / なしのどちらでも BC-768 を検出できた。

```text
[SCAN]
name=BC-768
id=<masked>
rssi=-70
services=<SERVICE_UUID>
manufacturerData=nil
serviceData=nil
connectable=1
matchesServiceUUID=true
```

- 広告に `SERVICE_UUID` が含まれるため、名前に依存せず Service UUID で識別できる。
- Manufacturer Data / Service Data は広告されない。
- 広告される名前は `BC-768` の後に空白が付く。名前一致で判定してはいけない根拠のひとつ。

### probe — Phase 1

| 項目 | 結果 |
| --- | --- |
| Connect | 成功（約 0.7 秒） |
| Service Discovery | Service は 1 件のみ。`SERVICE_UUID` に一致 |
| Characteristic Discovery | 5 件すべて発見（`VERIFY: configured=5 found=5 missing=nil propertyMismatch=nil`） |

実測した Property は設定と一致した。

| 論理名 | Properties | Descriptors |
| --- | --- | --- |
| `WRITE_CHAR_1` | `writeWithoutResponse` | なし |
| `WRITE_CHAR_2` | `writeWithoutResponse` | なし |
| `NOTIFY_CHAR_1` | `notify` | `2902` (CCCD) |
| `NOTIFY_CHAR_2` | `notify` | `2902` (CCCD) |
| `NOTIFY_CHAR_3` | `notify` | `2902` (CCCD) |

`read` 可能な Characteristic は 1 つも存在しない。標準の Device Information Service 等も公開されていない。
したがって「read によって Pairing を誘発する」経路は BC-768 には存在しない。

**Phase 1 は達成。**

### probe — Phase 2（Case B: Notify 購読）

```text
[NOTIFY_STATE] logical=NOTIFY_CHAR_1 enabled=true error=nil
[NOTIFY_STATE] logical=NOTIFY_CHAR_2 enabled=true error=nil
[NOTIFY_STATE] logical=NOTIFY_CHAR_3 enabled=true error=nil
```

- CCCD への書き込みは 3 件ともエラーなしで成功した。**認証・暗号化は要求されなかった。**
- macOS の Pairing UI は表示されなかった。
- Notify は 1 件も受信しなかった。
- 購読の約 11 秒後、Peripheral 側から切断された。

```text
[DISCONNECTED]
error=The connection has timed out unexpectedly. (domain=CBErrorDomain code=6)
```

**Phase 2 は未達。**

### 考察

- BC-768 の Notify Characteristic には Security Requirement が設定されていない。CCCD 書き込みが素通りするため、
  **Notify 購読だけでは macOS 側の Pairing は誘発されない**（仕様書 Case B は否定された）。
- GATT に read 可能な属性がないため、read 経由での誘発も不可能。
- 残る誘発経路は `WRITE_CHAR_1` / `WRITE_CHAR_2` への Write（Case C）だが、正しい payload が確定するまで
  実行しない。推測データは送らない。
- そもそも BC-768 が BLE Legacy Pairing を要求しない可能性もある。その場合「Pairing 待機状態の終了」は
  BLE の Bonding ではなく、独自プロトコルのハンドシェイク（Write → Notify）の完了を指していることになる。

### 次に試すこと

1. BC-768 の「設定/通信」ボタンを約 3 秒長押しして Pairing 待機状態にしたうえで `probe` を再実行し、
   接続維持時間と切断理由が変わるかを見る。
2. 同条件で `probe --no-subscribe`（Case A）を実行し、Connect のみで本体側の待機状態が終了するかを比較する。
3. `--wait` を長めに取り、11 秒での切断が Peripheral 側のアイドルタイムアウトかどうかを確認する。
4. Android の HCI ログから測定開始 Write の payload が確定したら Case C を検討する（Phase 4）。

## 2026-08-19 追加検証（Pairing 待機状態あり）

BC-768 の「設定/通信」ボタンを約 3 秒長押しし、Pairing 待機状態にしてから実行した。

### 実行順と結果

| # | 条件 | コマンド | 接続維持時間 | 切断理由 |
| --- | --- | --- | --- | --- |
| 1 | 待機状態**なし**（前回） | `probe --wait 45` | 約 11 秒 | `CBError code=6` (Peripheral 側) |
| 2 | 待機状態**あり** | `probe --wait 60` | **60 秒（切断されず）** | `--wait` 経過による自主切断 |
| 3 | #2 の直後に再接続 | `probe --wait 40` | 約 4.6 秒 | `CBError code=6` (Peripheral 側) |

### #2（Pairing 待機状態あり・Case B）

- Phase 1 は #1 と同じく成功。Service / Characteristic 構成に差はない。
- Notify 3 件の購読はすべて `error=nil` で成功。ここでも認証・暗号化は要求されなかった。
- **macOS の Pairing UI は表示されなかった。**
- **Notify の受信は 0 件。**
- 60 秒間、Peripheral 側から切断されなかった。

### #3（再接続 = Bonding 確認）

- scan で再発見・再接続はできたが、約 4.6 秒で `CBError code=6` により Peripheral 側から切断された。
- Pairing 待機状態でないときの挙動（#1）と同じ。**Bonding は成立していない。**

### 分かったこと

- 接続を維持できるかどうかは Pairing 待機状態か否かで決まる。待機状態でなければ数秒〜十数秒で
  Peripheral 側から切断される。BC-768 のアイドルタイムアウトではなく、待機状態のゲートと考えられる。
- **Notify 購読は Pairing のトリガーにならない**（待機状態の有無にかかわらず CCCD 書き込みが素通りする）。
  仕様書 Case B は待機状態ありでも否定された。
- macOS の Pairing UI が一度も出ないことから、**BC-768 は BLE Legacy Pairing / Bonding を要求していない**
  と考えられる。仕様書が言う「Pairing 待機状態の終了」は BLE の Bonding ではなく、
  独自プロトコル（`WRITE_CHAR` への Write → `NOTIFY_CHAR` での応答）のハンドシェイク完了を指す可能性が高い。
- BC-768 は待機状態の間、Write を待っている。こちらから何も送らないため、Notify も返らない。

### 次に試すこと

1. Case A（`probe --no-subscribe`）を待機状態で実行し、Notify 購読の有無で接続維持時間が変わるか確認する。
   変わらなければ「Notify 購読は接続維持に無関係」が確定する。
2. Android の HCI キャプチャから、Health Planet が待機状態の BC-768 へ最初に送る Write payload を特定する。
   これが分かるまで Case C（Write）は実行しない。
3. payload が確定したら、`WRITE_CHAR_1` / `WRITE_CHAR_2` への Write を実装し（Write Logger 付き）、
   Notify の応答を raw で記録する（Phase 4）。

## 2026-08-19 追加検証（Case A: Connect のみ）

Pairing 待機状態にしたうえで `probe --no-subscribe --debug --wait 60` を実行した。Notify 購読も Write も行わない。

| # | 条件 | 接続維持時間 | 切断理由 |
| --- | --- | --- | --- |
| 2 | 待機状態あり + Notify 購読（Case B） | 60 秒（切断されず） | `--wait` 経過による自主切断 |
| 4 | 待機状態あり + **購読なし**（Case A） | **60 秒（切断されず）** | `--wait` 経過による自主切断 |

- Phase 1 は同様に成功。
- Notify 購読を行わなくても、待機状態であれば 60 秒間切断されなかった。
- macOS の Pairing UI は表示されず、Notify も 0 件（購読していないので当然）。

### 結論（Pairing 検証 Case A / B）

| 観点 | 結果 |
| --- | --- |
| 接続維持を決めるもの | **BC-768 の Pairing 待機状態のみ**。Notify 購読の有無は無関係 |
| Notify 購読による Pairing 誘発 | **発生しない**（Case B 否定） |
| Connect のみによる待機状態の終了 | **発生しない**（Case A 否定） |
| macOS の Pairing UI | 一度も表示されない |
| Bonding | 成立しない（再接続時に待機状態なしと同じ挙動へ戻る） |

BC-768 は GATT レベルの Security Requirement を一切設定していない。CoreBluetooth 側から
「アクセスによって Pairing を誘発する」経路（read / notify）は存在せず、read 可能な Characteristic も無い。

したがって残る経路は `WRITE_CHAR_1` / `WRITE_CHAR_2` への Write（Case C / Phase 4）のみ。
正しい payload が Android HCI ログから確定するまで実行しない。

仕様書 22 の Phase 2「Notify enable → macOS Pairing 開始 → BC-768 Pairing 待機終了」は、
**成立しない前提だったと結論づける**。Phase 3（Bonding 済み再接続）も同じ理由で現状は到達不能。

## 2026-08-19 handshake の実機検証

`docs/protocol.md` で特定した手順を実際に送った。送ったのは HCI ログで観測済みの payload のみ。

### 確定したこと

| 項目 | 結果 |
| --- | --- |
| 送信先 Characteristic | **`WRITE_CHAR_1`**（HCI ログの ATT handle `0x001A` に対応） |
| 通知元 Characteristic | **`NOTIFY_CHAR_3`**（同 `0x0017`。CCCD は `0x0018`） |
| フラグメント / チェックサム | 実機で受理された。プロトコル解析は正しい |
| `0x0003` の送信 | 3 フラグメントに分割して送信し、**応答 `0x8003` が返った** |

Notify は 3 本購読していたが、実際に通知が来たのは `NOTIFY_CHAR_3` だけだった。
HCI ログで Android が 1 本しか購読していなかったことと一致する。

### 詰まっているところ

```text
→ 0x0003 <client id>   ← 0x8003 payload=0701   （Android は 4 セッションとも payload=0000）
→ 0x0010 <payload>     ← 応答なし → 約 3 秒後に BC-768 側から切断（CBError code=7）
```

`0x8003` の payload が Android と異なる。2 回実行して同じ結果になった（再現性あり）。

### 判明した BC-768 の広告条件

長押しをしていない状態では **BC-768 は広告を出さない**（Service UUID フィルタありの scan で 150 秒 0 件）。
接続できるのはボタン操作をした後だけ。

### 仮説と次に試すこと

`0701` が何を意味するかは未確定。差分として残っているのは以下。

1. **Notify の購読本数**。Android は 1 本のみ、こちらは 3 本購読していた
   → `--notify-char notify3` で Android と揃えられるようにした。
2. **タイミング**。Android は接続から CCCD 書き込みまで約 1.6 秒、そこから `0x0003` まで約 0.2 秒。
   こちらは接続から 0.3 秒で送っている → `--handshake-delay` で待てるようにした。
3. **BC-768 の動作モード**。長押しで入るモードと、Android がデータ送信していたモードが
   違う可能性がある。Android のキャプチャがどちらの状態で取られたかは未確認。
4. `0x0010` の payload に含まれるカウンタ。HCI ログの 4 セッションを比べると
   **0.5 秒刻み（2 カウント/秒）で単調増加**していた。基準時刻が不明なため現状は古い値をそのまま送っている。
   ただし `0x0003` の時点で応答が異なるため、これは直接の原因ではない。

## 2026-08-19 Android 条件に揃えた再試行と本体エラー表示

### 試したこと

`--notify-char notify3`（Android と同じく 1 本だけ購読）と `--handshake-delay 1.6`
（Android の接続→送信間隔を再現）を付けて再実行した。送信 payload は変えていない。

### 結果

```text
→ 0x0003 <client id>   ← 0x8003 payload=0701   （変化なし）
→ 0x0010 <payload>     ← 応答なし → BC-768 側から切断
```

購読本数もタイミングも `0701` に影響しなかった。以下の 2 つの仮説は否定された。

- ~~Notify を 3 本購読していたのが原因~~
- ~~接続からハンドシェイク開始までが速すぎたのが原因~~

### BC-768 本体のエラー表示 → 原因が判明

この試行の前後で、**BC-768 本体に `ErCP` が表示され、その後電源が切れた**
（7 セグ表示のため当初 `ErC7` と読んだが、取扱説明書の一覧と照合して `ErCP` と判断した）。

取扱説明書「故障かな？と思ったら」の記載。

| 表示 | 症状 | 対処 |
| --- | --- | --- |
| `ErCP` / `ErCd` | 本機とスマートフォンが接続できない | 電池、通信圏内（5m）、Bluetooth がオンか |
| `ErCP` が何度も | — | **スマートフォンの設定から本機の登録を解除し、再度本機の登録をする** |
| `ErCU` | — | 本機で個人データを消去した場合。登録を解除して再登録する |
| `ErCg` | 接続できない | 電池を入れ直す |

`ErCP` は**接続が確立できなかったことを示す想定内の表示**で、機器の故障ではない。
電池を抜く必要があるのは別コード（`ErCg`）。

### 結論：macOS は「未登録の端末」として拒否されている

`0x8003` の payload `0701` は、**登録されていない端末からのセッション開始を拒否した応答**と解釈できる。

- 取扱説明書が「登録を解除して再度登録する」という手順の存在を明示している。
  つまり BC-768 には**端末の登録フローがある**。
- Android の識別子文字列をそのまま送っても通らないのは、BC-768 が登録済み端末を
  識別子だけでなく BLE アドレス等とセットで記憶しているためと考えられる。
- 今回のキャプチャ 4 セッションはすべて**登録済み端末による通常のデータ送信**で、
  登録フローそのものは含まれていない。

送った payload はすべて HCI ログで観測済みのものであり、推測データは送っていない。
それでも拒否されたのは、前提となる「端末登録」が済んでいないためである。

### 次に必要なもの

**Health Planet で BC-768 の登録を解除し、再登録する操作の HCI キャプチャ。**

これがあれば登録シーケンス（`0x0003` の前後に何を送るか、`0x0002` 等の別コマンドがあるか）が
そのまま分かる。現状の情報だけで登録手順を推測して送ることはしない。

## 2026-08-19 BC-768 の測定成功フロー（実機操作の記録）

Android の Health Planet で測定に成功した際の操作手順。

```text
1. アプリから起動する         → BC-768 が起動するが、一旦電源が切れる
2. 本体の「入力モード」を押す  → 入力モードで起動
3. 待機
4. 乗る                      → 測定成功（アプリに結果が出る）
```

手順 1 で一度電源が落ちるのは、macOS から接続したときに `ErCP` で落ちたのと似た挙動に見える。
測定を成立させるには本体側で入力モードに入れる操作が必要らしい。

### この回の HCI ログは取得できていない

バグレポートを確認したところ、`persist.bluetooth.btsnooplogmode` が `disabled` のままだった。
`bt_hci_snoop_log` の設定画面を操作したログは残っているが、記録は開始されていない。

Android では開発者オプションで有効化した後、**Bluetooth を OFF → ON するか端末を再起動しないと
HCI スヌープが始まらない**。次回取得時はここに注意する。

なお `dumpstate_log-*.txt` は bugreport 生成の進行ログ、`bugreport-*.tmp` はテキスト形式の
bugreport 本体で、どちらにもバイナリの `btsnoop_hci.log` は含まれない。
必要なのは bugreport zip 内の `FS/data/misc/bluetooth/logs/btsnoop_hci.log`（または `.filtered`）。

Health Planet アプリ自身は測定値を logcat に出していないため、bugreport のテキストからは
体組成値を得られない。

## 2026-08-19 measure 成功（Phase 4 達成）

`bc768-probe measure` で、macOS から BC-768 の測定を実行し測定結果を受信できた。

### 実行条件

| 項目 | 内容 |
| --- | --- |
| BC-768 の状態 | 本体の**「入力モード」**を押して起動（設定/通信ボタンの長押しではない） |
| Health Planet | 起動しない |
| `0x0010` の payload | **実行時の現在時刻から生成**（固定値をやめた） |

### 結果

全 8 ステップが応答を得て完走した。

```text
identify     0x0003 <client id>  → 0x8003 payload=0000   ← 0701 から 0000 に変わった
session      0x0010 <日時>        → 0x8010 payload=0000
device-info  0x0020 00           → 0x8020 57 bytes（モデル名 BC-768 を含む）
read-data    0x1000 00           → 0x9000 43 bytes
measure      0x2010 00           → 0xA010 payload=0000   ← 送信から 10.6 秒後（この間に乗る）
complete     0x3000 00           → 0xB000 payload=0001   ← 測定データありを示す
result       0x3010 01           → 0xB010 87 bytes       ← 測定結果を受信
finish       0x0001 00           → 0x8001 payload=0000
```

送信先は `WRITE_CHAR_1`、通知は `NOTIFY_CHAR_3`。切断はセッション終了後に BC-768 側のタイムアウト（`code=6`）。

### `0701` の原因は BC-768 の動作モードだった

`0x0003` の応答が `0701` → `0000` に変わった。`0x0003` の payload は変えていないので、
差は **BC-768 側の動作モード**である。

| BC-768 の操作 | `0x0003` の応答 | 結果 |
| --- | --- | --- |
| 設定/通信ボタンを約 3 秒長押し | `0701` | 拒否され、本体に `ErCP` が出て電源が切れる |
| **「入力モード」を押す** | `0000` | **受理され、測定まで完走する** |

長押しで入るのは設定/通信モードであり、測定セッションを受け付けるモードではなかった。
「未登録の端末だから拒否された」という以前の推測は誤りで、**端末登録は不要**だった。
Android の識別子をそのまま使えば受理される。

日時の修正（固定値 → 現在時刻）は `0x0010` を通すために必要だった。
以前 `0x0010` に応答がなかったのは、その前の `0x0003` が拒否されていたためでもある。

### 到達状況

| Phase | 状態 |
| --- | --- |
| Phase 1（発見・接続・Discovery） | 達成 |
| Phase 2（Pairing 誘発） | **不要と判明**。BC-768 は BLE Pairing を要求しない |
| Phase 3（Bonding 再接続） | 同じ理由で不要 |
| Phase 4（測定開始 → raw payload 受信） | **達成**。87 バイトの測定結果を取得 |

残るのは測定結果 87 バイトの TLV デコード（体組成値の解釈）。

## 2026-08-19 デコード実装後の再計測

`bc768-probe measure` を再実行し、全 8 ステップ完走のうえで測定結果のデコードと検算まで確認した。

| 項目 | 結果 |
| --- | --- |
| ハンドシェイク | 8 ステップすべて `STEP_OK` |
| `0xB010` の解釈 | TLV 23 個、末尾まで既知タグで解釈（未解釈バイトなし） |
| BMI の検算 | 一致 |
| 除脂肪量の検算 | 一致 |
| `6A33` の時刻 | 実行時刻と秒単位で一致 |

前回の測定（約 12 分前）と比べ、体重・体脂肪率・筋肉量・推定骨量・基礎代謝・体水分率が
それぞれ整合の取れた方向に動いた。身長・体内年齢・内臓脂肪レベル・判定値らしき小さな定数は不変。
係数の解釈が正しくなければ、2 回の測定間でこの整合は崩れる。

これで「測定 → raw payload 受信 → 主要項目のデコード」が macOS 単独で完結することを確認できた。

## 2026-08-19 データ取得（`0x2010` を送らない場合）の挙動

測定を開始せず `0x3000` → `0x3010` だけを送って、BC-768 が保持しているデータを取得できるか確かめた。
直前の `measure` で `0x3010` を実行済みの状態から実施。

### 結果

```text
→ 0x3000 00   ← 0xB000 payload=0000        取り出せるデータなし
→ 0x3010 01   ← 0xB010 87 bytes            それでも応答する
```

返ってきた 87 バイトは、**直前の測定と同じ測定値**で、**日付・時刻がともにゼロ**、
先頭ヘッダも測定直後の `0x0001` ではなく `0x0501` だった。

| 状況 | `0xB000` | `0xB010` ヘッダ | 日付・時刻 | 測定値 |
| --- | --- | --- | --- | --- |
| 測定直後 | `0001` | `0x0001` | 実測時刻 | 今回の値 |
| 測定していない | `0000` | `0x0501` | **ゼロ** | **前回値がそのまま** |

BC-768 は最後の測定値をバッファに保持しており、`0x3010` は無条件に応答する。
これを知らずに実装すると、古い測定値を新しい結果として取り込んでしまう。

対応として、`0xB000` の payload で有無を判定し（`0001` / `0000`）、
さらに `0xB010` の日付・時刻がゼロなら `ERROR` で警告するようにした。

### `0x9000` の日時が古いままだった

同じセッションで `0x9000` の日時が **01:53:08** を指していた。これは Android が最後に
`0x1002`（設定の書き込み）を送った時刻である。`bc768-probe` は `0x1002` を送らないため更新されない。

つまり **BC-768 に保存済みの設定（身長 174.0 cm、性別、年齢）に依存して測定が成立している**。
設定が入っていない機体で測定できるかは未検証。

### 未検証のまま残っていること

**本体単体で測定したデータを、後から引き取れるか。** これを確かめるには、ツールもアプリも
起動していない状態で BC-768 だけで測定し、その後に `sync` で接続して `0xB000` が `0001` を
返すかを見る必要がある。返れば「測定」と「引き取り」を完全に分離できることが確定する。

## 2026-08-19 動作モードと Health Planet の画面の対応が判明

### 本体の操作とアプリ画面の対応

| 本体の操作 | 対応する Health Planet の画面 | `0x0003` の応答 |
| --- | --- | --- |
| 電源を押す | HOME 画面 | `0000` 受理 |
| 「入力モード」を押す | 入力画面 | `0000` 受理 |
| 設定/通信ボタン長押し | — | `0701` 拒否（`ErCP`） |

**以前の記述を訂正する。** 「入力モードでないとセッションが拒否される」と書いていたが、
実際に拒否されるのは**設定/通信ボタン長押しのモードだけ**だった。
電源 ON の状態でもハンドシェイクは `0000` で受理され、`sync` が完走する（03:07 と 03:11 に実測）。

### Android のキャプチャは「データ確認」だった

HCI キャプチャの測定なし 4 セッションは、`0x3000` → `0xB000` = `0000` を受けて
`0x3010` を送らずに終わっていた。これは HOME 画面を開いたときの自動データ確認に相当する。

アプリが毎回 `0x3000` で**データの有無を確認している**ことから、
BC-768 に測定データが保持されるケースが存在すると考えられる。
`bc768-probe sync` はこの動作をそのまま実装したものになっている。

### プロファイルなしの測定では溜まらない

電源 ON だけで起動して乗る測定はプロファイルなしの測定で、体組成の算出ができず
本体に `ErC5`（7 セグ表示のため `ErCS` の可能性もある）が出て失敗した。
この状態では当然データも溜まらないため、この条件での `sync` 検証（`0xB000` = `0000`）は
**判断材料にならない**。

### 次に確かめること

**入力モード（プロファイルあり）で、接続相手なしに測定できるか。**

```text
1. ツールを起動しない
2. 「入力モード」を押す
3. 乗って測定する
   → 体組成の結果が出る  : その後 sync で 0xB000 が 0001 になるか確認できる
   → すぐ電源が落ちる    : プロファイル測定は接続下でしか成立しない = データは溜まらない
```

どちらに転んでも結論が出る。落ちる場合、「データ確認」で引き取れるのは
「接続下で測定したが引き取る前に切断された」ケースだけになる。

`ErC5` / `ErCS` の意味は取扱説明書で未確認。

## 2026-08-19 単体測定データの引き取り（本命の検証）

### 手順

```text
1. ツールを起動しない状態で「入力モード」を押す
2. 乗って測定する → 測定できた（プロファイルあり）
3. bc768-probe sync で接続し、電源を押す（乗らない）
```

### 結果

`0xB000` = `0000`（送信対象なし）。しかし `0x3010` の**中身が前回と変わっていた**。

| 項目 | 直前の sync (03:12) | 今回 |
| --- | --- | --- |
| 体重 | 変化なし | 変化なし |
| 体脂肪率 | — | **更新** |
| 筋肉量 | — | **更新** |
| 推定骨量 | — | **更新** |
| 基礎代謝 | — | **更新** |
| 体内年齢 | — | **更新** |

除脂肪量の検算も新しい値どおりに成立した。つまり**単体測定の結果がバッファに入っている**。
前回値の残留ではない。

日付・時刻はともにゼロだった。

### 結論：BC-768 は時計を持っていない

`0x0010` で接続のたびに日時を渡すのは、本体に時計がないためと考えられる。
接続外で測定すると値は残るが日時が付かず、`0xB000` も立たない。

したがって `0xB000` は「取り出せるデータの有無」ではなく
**「タイムスタンプ付きで送信対象になるデータがあるか」**を示す。
日時のないデータは Health Planet 側で記録できないため対象外になる、という理解で整合する。

### アプリに落とし込む場合

| 機能 | 実現方法 |
| --- | --- |
| 入力 | `measure`。接続下で `0x2010` を送る。**日時が付く唯一の方法** |
| データ確認 | `sync`。`0xB000` が `0001` なら日時付きで引き取れる。`0000` でも値は読めるが測定時刻が不明 |
| データ送信 | 取得したデータを保存・送信する（ツール側の実装） |

単体測定した結果を後から正確な時刻付きで引き取ることはできない。
測定時刻が必要なら `measure` で接続下に測るしかない。

### 未確認

- 単体測定を 2 回した場合、バッファに残るのは最後の 1 件だけか
- `0xB000` が `0001` になる条件（接続中に測定して引き取る前に切断した場合など）
- `ErC5` / `ErCS` の意味（取扱説明書で未確認）

## 2026-08-19 JSON 出力の実機確認

`bc768-probe measure --json | jq .` で、測定から JSON 出力までを通しで確認した。

| 項目 | 結果 |
| --- | --- |
| `measuredAt` | BC-768 が返した日時から復元。実際に乗り終わった時刻と一致 |
| `retrievedAt` | 取得時刻。`measuredAt` との差は約 20 秒（測定完了から引き取りまで） |
| `sendPending` | **`true`（初観測）**。接続下で測定したため `0xB000` が `0001` を返した |
| `hasTimestamp` | `true` |
| `checks` | BMI・除脂肪量ともに `passed: true` |

`sendPending` はこれまで `false` しか観測していなかったため、両方の値で判定が働くことを確認できた。

### パイプでの注意点

`--wait` を明示すると待機し続けるため、パイプ先（`jq` など）が出力をフラッシュできず
JSON が画面に出てこない。既定では手順の完走時点で終了するようにした。
接続を保って観測したい場合は `--wait 0` を使う。

## 2026-08-19 引き取り後の `sync`（日時クリアの確認）

`measure` で測定・引き取りをした直後に `sync` を実行した。

| 項目 | 結果 |
| --- | --- |
| `sendPending` | `false`（`0xB000` = `0000`） |
| `hasTimestamp` | `false` |
| `measuredAt` | `null` |
| ヘッダ | `0x0001` → **`0x0501`** |
| 測定値 | 直前に引き取った値がそのまま残っている |

**`0x3010` で引き取ると日時がクリアされる**ことが確認できた。値はバッファに残るが、
日時が消え `0xB000` も落ちる。二重取得を防ぐ仕組みと考えられる。

これで「日時ゼロ」の意味が確定した。

- 引き取り済み
- または接続外で測定された

どちらも「新規データではない」ことを示す。したがって実装の判定は
**`sendPending == true` かつ `hasTimestamp == true`** のみを新規として扱えばよい。
重複も取り逃しも起きない。

## 2026-08-19 新規クライアント識別子の可否

GUI から設定を自動登録できるか確かめるため、`uuidgen` で作った識別子を `0x0003` で送った。

```bash
BC768_CLIENT_ID=$(uuidgen | tr 'A-Z' 'a-z') bc768-probe handshake
```

### 結果

`0x8003` の payload が **`0001`**（これまで観測していなかった値）。その後 `0x0010` に応答がなく停止した。

| payload | 状況 |
| --- | --- |
| `0000` | Health Planet が使っている識別子 → 受理 |
| **`0001`** | **新規生成した識別子 → 未登録として拒否** |
| `0701` | 設定/通信ボタン長押しのモード → 拒否 |

**識別子は BC-768 側に登録されている必要があり、任意の値では通らない。**

### 実装のバグが見つかった

応答コマンドが期待どおりなら `STEP_OK` としていたため、payload が拒否を示していても
成功扱いで次のステップへ進んでいた。`BC768ResponseStatus` を追加し、
状態コードが `0x0000` 以外なら理由を出して停止するようにした。

### GUI の設定登録への影響

- Service / Characteristic UUID は接続して Discovery すれば自動で取れる
- **クライアント識別子は自動生成できない。** HCI キャプチャから取った値を手入力する必要がある
- 登録手順（取扱説明書が言う「本機の登録」）が解析できれば自動化できる

## 2026-08-19 別プロファイルでの新規識別子

BC-768 側で別のプロファイル（個人番号）を選び、新しく生成した識別子で `0x0003` を送った。

### 結果

`0x8003` の payload は **`0001`**（未登録）のままだった。

**クライアント識別子はプロファイル単位ではなく、機器単位で管理されている。**
個人番号を変えても、未登録の識別子は受け付けられない。

修正した応答検査（`BC768ResponseStatus`）は期待どおり働き、`RESPONSE_REJECTED` を出して停止した。

### 分かっている広告情報

LightBlue で確認したところ、広告には次が載っている。

| 項目 | 値 |
| --- | --- |
| Local Name（広告） | `TNT_BW` |
| Service UUIDs（広告） | 独自 Service 1 つ |
| Connectable | Yes |

接続後に読める Device Name は `BC-768`（末尾に空白 2 つ）。両方とも機器の識別に使える。
Service / Characteristic UUID は機種共通で、Discovery で自動取得できるため設定不要にできる。

### 他人が使えるようにするために残っていること

**登録フローの解析だけ。** UUID は自動取得でき、識別子だけが機器に登録済みである必要がある。
Health Planet で機器登録をやり直すときの HCI キャプチャがあれば、
アプリから新しい識別子を登録できるようになり、設定なしで使えるようになる。

## 2026-08-19 UUID は事前知識なしで取得できる（実証）

`discover` コマンドを作り、**UUID 関連の環境変数をすべて外し、設定ファイルも読まない状態**で実行した。
手がかりは広告名の部分一致（`TNT`）だけ。

### 結果

```text
localName=TNT_BW    name=BC-768    services=1  characteristics=5

Service（1 つだけ）
  …510F  [notify]
  …5108  [writeWithoutResponse]
  …510E  [notify]
  …5107  [writeWithoutResponse]
  …510D  [notify]

→ 構成に一致。UUID 昇順で割り当て
  SERVICE_UUID / WRITE_CHAR_1,2 / NOTIFY_CHAR_1,2,3
```

**割り当て結果は、あらかじめ与えられていた値と完全に一致した。**

CoreBluetooth が返す Characteristic の順序はばらばらだが（`510F, 5108, 510E, 5107, 510D`）、
properties で write / notify に分類してから UUID 昇順に並べ直すため、順序に依存しない。

### 判定条件

BC-768 は次の構成を持つ。これに一致すれば UUID を知らなくても割り当てられる。

- Service が 1 つだけ（標準サービスも公開していない）
- その配下に writeWithoutResponse が 2 本、notify が 3 本、合計 5 本

広告からは Service UUID と `TNT_BW` が読め、接続後の Device Name は `BC-768`。
いずれも認証なしで読める（BC-768 は BLE Pairing を要求しない）。

### 結論

**設定に UUID を書く必要はない。** 他人の環境でも `discover --save` で自動生成できる。
残る個体依存はクライアント識別子だけで、これは機器に登録済みの値でなければ
`0x0003` が `0001`（未登録）で拒否される。

## 2026-08-19 使われていない Characteristic の検証

5 本のうち通信に登場するのは `WRITE_CHAR_1` と `NOTIFY_CHAR_3` だけだが、
残り 3 本が「使えない」のか「使われていないだけ」なのかを確かめた。

### 記録からの集計

| 経路 | 回数 | 内訳 |
| --- | --- | --- |
| macOS で受信した Notify | 104 | **すべて `NOTIFY_CHAR_3`** |
| macOS で送信した Write | 71 | すべて `WRITE_CHAR_1`（こちらが選んだため） |
| Android の Notification（HCI 3 ログ） | 123 | すべて handle `0x0017` = `NOTIFY_CHAR_3` |
| Android の WriteCommand（同） | 90 | すべて handle `0x001A` = `WRITE_CHAR_1` |

macOS 側は `NOTIFY_CHAR_1/2/3` を **3 本とも購読した状態**で通信していた。
受信側が絞っていないのに 104 回すべて `NOTIFY_CHAR_3` に届いている。
Android は `NOTIFY_CHAR_1/2` の CCCD へ書き込みすらしていない。

→ **`NOTIFY_CHAR_1` と `NOTIFY_CHAR_2` は通知を送ってこない。**

### `WRITE_CHAR_2` への送信（実験）

`--write-char write2 --steps identify` で `WRITE_CHAR_2` へ `0x0003` を送った。

```text
→ WRITE_CHAR_2 へ 3 フラグメント
← NOTIFY_CHAR_3 で 0x8003 payload=0000（受理）
```

**受理された。** `WRITE_CHAR_2` もコマンドを受け付ける。応答は write 先に関わらず
`NOTIFY_CHAR_3` に返る。

### 結論

| Characteristic | 状態 |
| --- | --- |
| `WRITE_CHAR_1` | Android・macOS とも常用 |
| `WRITE_CHAR_2` | **使えるが、Android は一度も使っていない** |
| `NOTIFY_CHAR_1` / `NOTIFY_CHAR_2` | 購読しても通知が来ない |
| `NOTIFY_CHAR_3` | 唯一の通知経路 |

「使われていない」と「使えない」は別で、write については前者だけが成立する。
2 本ある理由は不明（冗長化か、未使用の用途か）。
実装は Android に合わせて `WRITE_CHAR_1` を既定にしておく。

## 2026-08-19 未確定タグの解析（体水分量・基礎代謝）

6 サンプル（重複を除くと 4 測定）のタグ値を並べ、変化するものと生理学的な関係を調べた。

### `6F21` は体水分率ではなく体水分量だった

| 解釈 | 除脂肪量との比 | 判定 |
| --- | --- | --- |
| 体水分量（kg） | **72.4 / 72.4 / 74.2 / 74.5 %** | 除脂肪組織の水分比率 72〜74% と一致 |
| 体水分率（%） | 64.0 / 63.9 / 65.5 / 65.7 % | 標準から外れる |

体水分量（0.1 kg 単位）が正しい。検算式として実装した。

### `6027` は筋肉量に比例する

```text
1902 / 63.10 = 30.143 kcal/kg
1913 / 63.50 = 30.126
1895 / 62.90 = 30.127
1896 / 62.90 = 30.143
```

4 測定で ±0.02% 以内。基礎代謝として確定とみなし、検算式に加えた。

### `614B` / `614C` は BIA のインピーダンス成分らしい

```text
R  = 388.1  381.8  374.5  377.8  Ω
Xc = -49.0  -48.4  -43.8  -45.0  Ω
```

足-足式の下半身測定として妥当な範囲。R が下がると体水分量が増える傾向も原理と合う。
裏づけが取れていないため推定のまま。

### 変化しなかったタグ

`6024`=3、`6025`=135、`602F`=8、`605A`=4、`6070`=4、`6077`=3、`607D`=2 は全測定で不変。
判定区分（「標準」「やや高い」など）と思われるが未解析。`6F22` も 12984〜12992 でほぼ一定。

### 検算は 4 本になった

```text
BMI      = 体重 / 身長²
除脂肪量 = 体重 × (1 - 体脂肪率) ≒ 筋肉量 + 推定骨量
体水分量 / 除脂肪量 = 0.72〜0.75
基礎代謝 / 筋肉量   = 30.1 kcal/kg
```

実データで 4 本すべて一致することを確認済み。
